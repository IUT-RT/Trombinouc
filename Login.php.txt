<?php

$auth = array( 'clement'=>"giaime",
				'claire'=>"giaime");
		
session_start();
if(empty($SESSION['user'])){//utilisateur connecter
	if(!empty($_POST['login']) && !empty($_POST['pwd']) && !empty($auth[$_POST['login']) && $auth[$_POST['login']==$_POST['pwd'])){
	$_SESSION['user']==$_POST['login'];
	}
else{
header('location:Login.php?msg=err');
exit();
}